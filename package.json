{
  "name": "cuppingcare",
  "version": "1.0.0",
  "description": "Cloudflare Worker for secure HLS video streaming with Firebase auth and R2 signed URLs",
  "main": "src/worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "aws4fetch": "^1.0.18"
  },
  "devDependencies": {
    "wrangler": "^4.38.0"
  },
  "keywords": [
    "cloudflare-workers",
    "hls",
    "video-streaming",
    "firebase-auth",
    "r2-storage"
  ]
}
